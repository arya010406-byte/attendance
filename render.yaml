services:
  - type: web
    name: school
    env: python
    buildCommand: "apt-get update && apt-get install -y cmake build-essential && pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13